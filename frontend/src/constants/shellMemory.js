/** xterm 滚动缓冲行数上限 */
export const SHELL_TERMINAL_SCROLLBACK = 500

/** 活跃 Shell 标签输出缓冲上限（字节） */
export const SHELL_OUTPUT_BUFFER_ACTIVE_MAX = 2 * 1024 * 1024

/** 后台 Shell 标签输出缓冲上限（字节） */
export const SHELL_OUTPUT_BUFFER_BACKGROUND_MAX = 512 * 1024
